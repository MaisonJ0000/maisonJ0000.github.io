import { css } from '@emotion/react';

const PopoverHeadStyle = css`
  text-decoration: underline;
  text-decoration-style: dotted;  
`;

const PopoverStyle = css`
  font-size: 12px;
`;

export {
  PopoverHeadStyle,
  PopoverStyle,
};
